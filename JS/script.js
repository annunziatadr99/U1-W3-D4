const newNum = [];
const numTombola = [];
for (let i = 0; i < 76; i++) {
  numTombola.push = i;
  return numTombola;
}
const createNum = () => {
  const tombola = document.getElementById("tombola");
  for (let i = 1; i < 76; i++) {
    newNum.push([]);
  }

  const numTombDiv = document.createElement("div");
  numTombDiv.className = "nums"; //oppure .claaslist.add
  const numTombH3 = document.createElement("H3");
  numTombH3.innerText = i;
};
window.onload = () => {};
